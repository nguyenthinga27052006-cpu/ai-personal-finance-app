# Production template only. Never commit real values or deploy this file directly.
APP_ENV=production
APP_IMAGE=registry.example/finance-assistant-api@sha256:REPLACE_AFTER_TRUSTED_BUILD
WORKER_IMAGE=registry.example/finance-assistant-worker@sha256:REPLACE_AFTER_TRUSTED_BUILD
DATABASE_URL=postgresql+psycopg://REPLACE_FROM_SECRET_MANAGER
HOST_DATABASE_URL=unused-in-runtime
REDIS_URL=redis://REPLACE_FROM_SECRET_MANAGER
JWT_SECRET=REPLACE_FROM_SECRET_MANAGER
NOTIFICATION_WORKER_TOKEN=REPLACE_FROM_SECRET_MANAGER
AI_PROVIDER=REPLACE_APPROVED_PROVIDER
AI_ENABLED=true
ENVIRONMENT_OWNER=platform-team
APPROVAL_REQUIRED=true
