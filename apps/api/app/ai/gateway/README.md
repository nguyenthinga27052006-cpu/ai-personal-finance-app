# Gateway Boundary

The executable gateway orchestration is in the sibling `gateway.py` module. This directory records the Phase 13 gateway responsibility boundary without introducing a second import path.
